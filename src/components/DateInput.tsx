export function DateInput({value,onChange,error}:{value:string;onChange:(value:string)=>void;error?:string}){const parts=value.split('-');const y=parts[0]||'',m=parts[1]||'',d=parts[2]||'';const change=(index:number,v:string)=>{const next=[y,m,d];next[index]=v;onChange(next.join('-'));};return <fieldset className="date-input"><legend>Ngày sinh</legend><div className="date-parts">{[{label:'Ngày',value:d,index:2,values:Array.from({length:31},(_,i)=>i+1)},{label:'Tháng',value:m,index:1,values:Array.from({length:12},(_,i)=>i+1)},{label:'Năm',value:y,index:0,values:Array.from({length:new Date().getFullYear()-1899},(_,i)=>new Date().getFullYear()-i)}].map(p=><label className="field" key={p.label}><span>{p.label}</span><select aria-label={`${p.label} sinh`} value={p.value} onChange={e=>change(p.index,e.target.value)} aria-invalid={!!error} aria-describedby={error?'date-error':undefined}><option value="">{p.label}</option>{p.values.map(v=><option key={v} value={String(v).padStart(2,'0')}>{v}</option>)}</select></label>)}</div>{error&&<p id="date-error" className="field-error" aria-live="polite">{error}</p>}</fieldset>;}
